<h2> <%= @user.name %>'s matches </h2>

<p>Hi <%=@user.name  %>, here are the people who hate what you hate! </p>

<%if @user.results.length == 0 %>
  <p>We can't find you a match if you haven't finished a <%= link_to("survey", surveys_path) %></p>
  <%else  %>
  <%count = 0 %>

  <div id="results">
<% @user.results.each do |result| %>
<% matches = result.make_matches(@user.match_generator(result))%>
<%sorted = matches.sort_by {|match| match[1].to_f}.reverse %>
<div class="matches" id =" <%=Survey.find(result.survey_id) %>">
  <p>Survey: <%= Survey.find(result.survey_id).name %></p>
<%sorted.each do |match| %>
<%user = User.find(match[0].to_i) %>
<p>You matched with <%= user.name %> with <%= number_to_percentage(match[1].to_f*100, precision:0) %> <br>
They are <%= user.status %>!</p>
<%= link_to("Visit Profile", user_path(user.id)) %> | <%= link_to("Chat Now!", chat_room_path(:receiver => user.id)) %> <br>
<%count+=1 %>
</div>
<% end %>
<%end %>
<%end %>
<br>
</div>
<%if count == 0 %>
<p> No matches yet! Have you checked out you <%= link_to("meetups", user_meetups_path(@user.id)) %>?</p>
<%end %>
